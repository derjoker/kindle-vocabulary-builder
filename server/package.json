{
  "name": "kindle-vocabulary-builder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "babel-node": "babel-node",
    "start": "nodemon --exec npm run babel-node -- src/server",
    "test": "jest --watch"
  },
  "dependencies": {
    "apollo-server": "^2.0.5",
    "graphql": "^14.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.18.4"
  }
}
